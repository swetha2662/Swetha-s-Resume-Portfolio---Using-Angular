export interface Education {
  institute: string;
  course: string;
  duration: string;
  score: string;
}

export interface Skill {
  name: string;
}

export interface Interest {
  name: string;
}
export interface Project {
  title: string;
  concepts: string;
  description: string[];
}
